# Color Queens 产品需求文档

最后更新：2026-07-05  
适用分支：`codex-iteration`  
当前阶段：Godot MVP

## 1. 文档目的

本文档用于记录 Color Queens 当前产品范围、功能模块、功能逻辑、实现原理和回归测试要求。后续每次功能迭代都必须同步更新本文档，并在正式发布前按本文档执行测试回归。

本文档是项目的产品事实源：

- 新增功能时，先补充或更新对应模块说明。
- 修改玩法、经济、界面、关卡或提示逻辑时，同步更新受影响模块。
- 发布前，以“回归测试清单”为准逐项验证。

## 2. 产品概述

Color Queens 是一款竖屏移动端休闲逻辑谜题。玩家在 6x6 彩色区域棋盘中放置皇冠，需要同时满足四条规则：

- 每一行只能有一个皇冠。
- 每一列只能有一个皇冠。
- 每个颜色区域只能有一个皇冠。
- 皇冠不能八方向相邻。

当前产品参考 Royal Match / Magic Sort 的休闲闯关外壳，但核心玩法保持 Queen / Star Battle 类逻辑谜题，不改为倒水、三消或排序玩法。

## 3. 目标用户与体验方向

目标用户是偏休闲的逻辑谜题玩家。产品体验目标：

- 上手轻：通过首页、关卡、提示和即时纠错降低理解成本。
- 逻辑清楚：提示要解释“为什么”，不能只告诉玩家答案。
- 节奏短：单关 6x6，适合碎片化游玩。
- 可持续迭代：关卡、提示、经济和主界面可以逐步扩展。
- 视觉轻快：整体参考录屏中的明亮休闲手游观感，使用浅天空色、明亮草绿色、柔和黄色和白色卡片，减少大面积深色和厚重阴影。

## 4. 当前版本范围

已实现：

- 休闲闯关首页。
- 6x6 Color Queens 棋盘玩法。
- 50 个默认关卡。
- 难度标记：新手、普通、困难、专家。
- 无猜解题路径数据：`logicStatus`、`hintSteps`、`solveSteps`。
- 教学提示：单候选、候选锁定、成组锁定、反证排除、直接冲突排除。
- 金币、提示次数、通关奖励。
- 本地存档。
- Godot 冒烟测试和新手教程冒烟测试。

未完成或占位：

- 生命系统目前只展示固定数值。
- 排行榜、活动、队伍、商店、宝箱为占位入口。
- 广告位为 Demo Placeholder。

## 5. 信息架构

主要页面：

- 首页：主视觉、开始关卡、新人流程入口。
- 新手引导教程：首次进入第一关前的强引导流程，依次教学放置皇冠、颜色区域唯一、横向和竖向唯一、相邻限制、提示使用和通关目标。
- 关卡页：棋盘、规则提示、撤销、清除、提示。
- 通关弹窗：通关反馈、金币奖励、下一关、重玩本关。

核心数据：

- `data/levels.json`：关卡配置、答案、难度、提示路径和无猜解题路径。
- `user://color_queens_save.json`：本地存档。
- 新手引导完成状态应写入本地存档，用于决定后续是否直接进入正式关卡。

## 6. 模块需求

### 6.1 首页模块

用户目标：明确当前在 Color Queens 主界面，看到主视觉和最核心入口，一键进入当前关卡；开发/验收时可快速模拟新人流程。

功能逻辑：

- 首屏显示 sky/garden/lake/path 风格背景和 Color Queens 主视觉。
- 首页不显示顶部金币、生命、星数、设置按钮。
- 首页不显示左侧每日奖励、宝箱入口。
- 首页不显示右侧活动、排行榜入口。
- 首页不显示底部星、杯、主页、队、设导航。
- 中下部只显示“开始关卡”和“新人流程”按钮。
- 首页进度根据当前关卡和已完成关卡数更新。
- “新人流程”按钮用于验收和调试，点击后模拟新用户状态，重新进入新手引导教程第 1 步。

当前实现：

- UI 由 `scripts/main.gd` 中 `_build_home_screen()` 及其子函数动态构建。
- 首页主操作由 `_build_home_primary_buttons()` 构建。
- “开始关卡”调用当前关卡流程；“新人流程”调用 `_simulate_new_user_flow()` 重置教程完成状态并进入教程。
- 资源、活动、排行榜、宝箱和底部导航入口已从首页隐藏。

功能截图与交互流转：

![首页模块截图与交互流转](prd_screenshots/01_home_module.svg)

回归测试重点：

- 点击开始关卡进入关卡页。
- 点击新人流程会重新进入新手引导第 1 步。
- 首页不显示金币、生命、星数、左侧入口、右侧入口和底部导航。

### 6.2 关卡页模块

用户目标：完成当前关卡，清楚知道关卡进度、剩余操作和规则反馈。

功能逻辑：

- 顶部栏只包含返回首页按钮；不显示生命、关卡下拉、设置或其它右上角按钮。
- 关卡标题只显示关卡编号，不显示关卡名称。
- 教练提示区显示关卡教程或当前提示解释。
- 棋盘占据主要空间，正式关卡底部为撤销、清除、提示按钮。
- 底部不显示广告占位。

当前实现：

- UI 由 `scripts/main.gd` 中 `_build_game_screen()` 及其子函数构建。
- 冲突状态由 `_validate_and_update()` 更新。
- 顶部编辑、设置、红心和底部广告入口已从关卡页移除。
- 新手引导页会隐藏帮助问号、关卡下拉和进度条。

功能截图与交互流转：

![关卡页模块截图与交互流转](prd_screenshots/02_level_module.svg)

回归测试重点：

- 页面在移动端宽度下按钮和棋盘不溢出。
- 关卡页不显示金币、红心、排行榜、编辑、设置和广告。
- 关卡标题只显示关卡编号。
- 新手引导页不显示“新手教程”标题、步骤名、帮助问号、关卡下拉和进度条。

### 6.3 棋盘交互模块

用户目标：通过单击格子标记排除、双击格子放置皇冠，直观看到颜色区域、皇冠、X、冲突和提示高亮。

功能逻辑：

- 单格基础交互：单击空白格标记 X，单击 X 取消排除；双击格子放置皇冠，双击皇冠或单击皇冠取消皇冠。
- 棋盘支持鼠标点击和触屏点击。
- 有冲突时冲突格变红。
- 提示时显示观察范围、候选、排除和建议格。
- 操作后提供轻量动画反馈。

当前实现：

- 交互状态由 `scripts/main.gd` 中 `cell_states` 管理。
- 棋盘绘制由 `scripts/game_board.gd` 自绘完成。
- `GameBoard` 通过 `cell_pressed(row, col)` 信号把点击传回主逻辑。
- 高亮类型包括 `unit`、`candidate`、`exclude`、`place`。
- `play_cell_feedback()` 和 `play_guide_feedback()` 提供格子反馈动画。

功能截图与交互流转：

![棋盘交互模块截图与交互流转](prd_screenshots/03_board_module.svg)

回归测试重点：

- 第一次点击为空格标记 X。
- 双击同格放皇冠。
- 第三次点击同格恢复空白。
- X、皇冠、高亮和冲突图形可见且不遮挡棋盘。
- 棋盘在不同窗口尺寸下保持正方形比例。

### 6.4 规则与冲突模块

用户目标：放错时能及时知道哪里违反规则。

功能逻辑：

- 任意两个皇冠如果同行、同列、同颜色区域或八方向相邻，则视为冲突。
- 开启即时纠错时，冲突格高亮。
- 即时纠错当前为内部开关，不提供前端设置入口。

当前实现：

- `_find_conflicts()` 遍历当前皇冠位置并检查四类冲突。
- `_validate_and_update()` 负责刷新错误状态、进度和教练提示。
- `immediate_errors` 控制是否显示即时错误。

功能截图与交互流转：

![规则与冲突模块截图与交互流转](prd_screenshots/04_rules_module.svg)

回归测试重点：

- 同行、同列、同区域、相邻两个皇冠均显示冲突。
- 关闭即时纠错后不显示红色冲突，但通关仍要求无冲突。

### 6.5 提示模块

用户目标：获得当前最值得判断的一步，并理解为什么可以选择或排除。

功能逻辑：

- 提示不直接替玩家放置皇冠。
- 提示消耗免费次数；免费次数用完后消耗金币。
- 免费提示初始为 3 次。
- 金币提示成本为 5。
- 提示给出文字解释和棋盘高亮。
- 提示应优先给出可以推进棋盘的逻辑判断。

当前提示策略顺序：

1. 单元唯一候选：某行、列或颜色区域只剩一个合法候选。
2. 候选锁定：某单元候选全部落在另一单元内，可排除另一单元其它候选。
3. 成组锁定：多个单元共同锁定一组行、列或颜色区域，可排除组外候选。
4. 反证排除：如果某格放皇冠，会导致某个单元没有任何可放位置。
5. 直接冲突排除：已有皇冠导致某格必然冲突。
6. 候选关注：没有确定推进时，提示值得比较的候选范围。

当前实现：

- `_use_hint()` 执行提示消耗、设置高亮和更新教练文本。
- `_build_best_next_hint()` 负责选择当前最佳提示。
- 相关策略函数集中在 `scripts/main.gd` 的提示逻辑区。
- 区域名称由 `REGION_COLOR_NAMES` 转换为可见颜色名，例如黄色区域、绿色区域。
- 关卡数据中的 `solveSteps` 记录无猜路径，但当前运行时提示主要按当前棋盘动态计算。

功能截图与交互流转：

![提示模块截图与交互流转](prd_screenshots/05_hint_module.svg)

回归测试重点：

- 点击提示不会自动放皇冠。
- 提示会消耗 1 次免费提示或 5 金币。
- 金币和免费提示不足时给出 toast。
- 提示文案必须解释原因，不只说“这里最优”。
- 提示高亮至少包含一个目标格。
- 对 50 关至少能从数据层证明唯一解和 no-guess 路径存在。

### 6.6 关卡数据模块

用户目标：获得稳定、可解、难度递进的关卡内容。

功能逻辑：

- 默认包含 50 个 6x6 关卡。
- 每关包含唯一答案。
- 每关必须标记难度。
- 每关必须标记 `logicStatus: no_guess`。
- 每关必须包含 `hintSteps` 和 `solveSteps`。

当前实现：

- 数据文件：`data/levels.json`。
- 加载器：`scripts/level_store.gd`。
- 基础校验包括行列尺寸、区域行列数和答案数量。
- 更完整的唯一解与路径校验目前由 `tests/smoke_test.gd` 覆盖。

功能截图与交互流转：

![关卡数据模块截图与交互流转](prd_screenshots/06_data_module.svg)

回归测试重点：

- `levels.size() >= 50`。
- 每关为 6x6。
- 每关答案满足行、列、区域唯一和不相邻。
- 每关唯一解。
- 每关存在难度、`logicStatus`、`hintSteps`、`solveSteps`。

### 6.7 通关与奖励模块

用户目标：完成关卡后获得明确反馈，并能进入下一关或重玩。

功能逻辑：

- 当皇冠数量等于目标数且无冲突时通关。
- 首次完成关卡奖励金币。
- 通关弹窗显示皇冠、完成标题、金币奖励、下一关和重玩按钮。
- 下一关按钮进入下一关；最后一关则回到首页。
- 重玩本关清空棋盘并重新开始。

当前实现：

- `_complete_level()` 处理完成状态、奖励、保存和弹窗。
- `WIN_REWARD` 当前为 10。
- `_next_level()` 和 `_replay_level()` 处理弹窗按钮。
- `completion_overlay` 为全屏遮罩弹窗。

功能截图与交互流转：

![通关与奖励模块截图与交互流转](prd_screenshots/07_completion_module.svg)

回归测试重点：

- 正确解完成后弹出通关弹窗。
- 首次通关金币增加 10。
- 已完成关卡重复通关不重复奖励。
- 下一关按钮尺寸足够明显且可点击。
- 重玩本关清空棋盘。

### 6.8 存档模块

用户目标：退出后能恢复当前进度。

功能逻辑：

- 保存当前关卡、棋盘状态、金币、提示次数、已完成关卡、完成状态和即时纠错开关。
- 保存新手引导状态，包括是否已完成、是否已开始、当前教程步骤。
- 启动时自动读取存档，用于恢复正式关卡进度或判断是否直接进入新手引导。
- 存档版本支持兼容迁移。

当前实现：

- 存档路径：`user://color_queens_save.json`。
- `_load_save()` 加载并兼容旧版本。
- `_save_game()` 在关键状态变化后写入。
- 当前保存字段包括 `currentLevelIndex`、`currentLevelId`、`cellStates`、`isCompleted`、`coinCount`、`hintCount`、`completedLevels`、`immediateErrors`、`tutorialCompleted`、`tutorialStarted`、`tutorialStepIndex`。
- `SAVE_VERSION` 当前为 3。

功能截图与交互流转：

![存档模块截图与交互流转](prd_screenshots/08_save_module.svg)

回归测试重点：

- 关卡切换、点击格子、使用提示、通关、新手教程推进后会保存。
- 重新启动后恢复最近关卡、棋盘状态和教程状态。
- 新存档首次启动会因为没有 `tutorialCompleted` 而直接进入新手引导。
- 旧版本存档不会导致启动失败。

### 6.9 新手引导教程模块

用户目标：在正式进入第一关前，用最短路径理解 Color Queens 的基础操作和核心规则，避免玩家第一次进入正式关卡时不知道如何点击、如何判断、如何求助。

触发与入口：

- 新用户首次安装或清除存档后，启动产品默认直接进入新手引导教程，不需要先点击首页按钮。
- 新手引导全部完成后，自动进入第 1 关正式关卡。
- 已完成新手引导的用户再次点击“开始关卡”时，直接进入当前正式关卡，不重复强制引导。
- 首页“新人流程”按钮可模拟新用户状态，重置教程完成状态并从新手引导第 1 步开始。
- 首页“新人流程”按钮可重新进入新手引导教程。
- 教程进行中，顶部显示“跳”按钮，点击后可二次确认跳过教程；正式关卡页不显示该按钮。

教程整体规则：

- 新人首次启动体验采用“视频式任务流”：启动后立即进入教程棋盘，先聚焦当前目标，再出现小手指引，用户完成动作后给成功反馈并推进。
- 新手引导使用独立教程棋盘或教程状态，不污染正式第 1 关的棋盘进度；基础规则关使用 4x4，合并规则关可使用 5x5。
- 每一步只开放当前需要学习的操作，其它按钮或格子应弱化或不可点，减少误触。
- 每一步应包含明确的目标文案、被操作区域或按钮高亮、成功反馈和自动推进；格子引导时先播放 0.3-0.5 秒目标闪光，再出现小手。
- 格子点击引导期间，棋盘应出现半透明蒙层：当前要求点击的目标格保持明亮高亮，其它格子被蒙层压暗；蒙层只做视觉引导，不应阻挡用户点击目标格。
- 按钮教学期间也必须采用单目标引导：当前按钮高亮，小手指向当前按钮；完成后小手移动到下一按钮或棋盘目标格。
- 引导词固定出现在棋盘上方教练提示区，教程阶段字号应明显大于正式关卡提示，并使用深色字体以提升可读性。
- 教程中出现错误操作时，应给出即时解释并保持在当前步骤，不惩罚玩家。
- 教程允许跳过；用户选择跳过后视为完成新手引导，并直接进入第 1 关正式关卡。
- 教程完成状态必须保存；用户中途退出并重新进入时，提供“继续教程”和“重新开始”两个选择。
- 教程文案统一使用“皇冠”，不在教程中使用 Queen 作为玩家可见称呼。
- 教程中隐藏返回首页按钮，顶部只保留“跳”按钮；正式关卡恢复返回首页按钮。
- 教程和正式关卡棋盘必须使用同一套清晰、鲜明的区域颜色体系，避免用户从教程进入正式关卡后产生视觉割裂。
- 第 1 到第 3 个教程关不显示底部撤销、清除、提示按钮；第 4 个教程关显示这三个按钮并逐个教学。
- 第 1 到第 3 个教程关完成后不展示正式关卡结算弹窗，只弹出轻量完成提示，2 秒后自动进入下一教程关卡；第 4 个教程关完成后显示“开始挑战”。

分步流程：

1. 引导放置皇冠。
   - 目标：让玩家先理解核心动作是在棋盘格中放置皇冠。
   - 教程操作：4x4 地图只聚焦一个目标格，棋盘显示蒙层且仅目标格保持明亮，小手指向目标格；玩家双击后目标格直接标注为皇冠。
   - 通过条件：玩家双击目标格放置皇冠。
   - 文案重点：初始显示“双击提示区，放置皇冠。”；成功后显示“皇冠放置成功”。

2. 引导颜色区域只能有一个皇冠。
   - 目标：让玩家理解同一个颜色区域里只能保留一个皇冠。
   - 教程操作：4x4 地图中某个颜色区域已经有一个皇冠，每次只高亮同色区域中 1 个还未排除的格子；小手逐个引导玩家把同色其它格点成 X。
   - 通过条件：该颜色区域中除皇冠外的其它同色格全部被标记为 X。
   - 文案重点：同一个颜色区域已经有皇冠，所以其它同色格都不能再放皇冠。

3. 引导皇冠周围和同行同列都不能有皇冠。
   - 目标：用同一个皇冠连续教学两个排除逻辑，先理解八方向相邻限制，再自然延伸到同行同列唯一。
   - 教程操作：在 5x5 教程棋盘中间预先放置一个皇冠。第一阶段按顺时针引导玩家把皇冠周围 8 个相邻格标记为 X；第二阶段继续引导玩家把该皇冠同行、同列剩下的 4 个格子标记为 X。
   - 通过条件：皇冠周围 8 个格子，以及同行同列剩下的 4 个格子全部被标记为 X。
   - 文案重点：先显示“皇冠周围不会出现皇冠，滑动排除周围的格子”；进入同行同列阶段后切换为“皇冠的同行同列不会出现皇冠，标记排除”。

4. 引导撤销、清除、提示和通关目标。
   - 目标：让玩家知道底部三个操作按钮分别做什么，并理解提示是解释逻辑而不是直接代打；同时知道怎样才算完成一关。
   - 教程操作：第 4 个教程关显示底部“撤销”“清除”“提示”按钮，按顺序引导玩家点击撤销、清除、提示；当前按钮需要高亮并显示小手指向，前两个按钮完成后解锁下一个按钮。撤销教学开始前，棋盘上默认有一个 X，点击撤销后该 X 恢复为空白；清除教学开始前，棋盘上默认有多处尝试标记，点击清除后全部恢复为空白；提示教学开始前，棋盘为空，点击提示后给出一个可执行的有效提示，高亮并用小手指向目标格；玩家按提示双击目标格后，目标格标注为皇冠。
   - 通过条件：玩家依次点击撤销、清除、提示，并能看到每个按钮带来的棋盘变化；点击提示后必须按提示完成一次棋盘操作；完成后显示“已经了解全部规则，开始真正的挑战吧！”和“开始挑战”按钮。
   - 文案重点：撤销可以把上一步 X 恢复为空白，清除可以清空当前所有尝试标记，提示会告诉你该观察哪里和为什么；按提示完成操作后显示“已经了解全部规则，开始真正的挑战吧！”。

完成与进入正式关卡：

- 第 1 到第 3 步完成后显示轻量完成提示，2 秒后自动进入下一教程关卡。
- 第 4 步完成提示操作后不自动进入正式关卡，必须显示“开始挑战”按钮；用户点击后进入第 1 关正式关卡。
- 用户点击“跳过教程”后显示二次确认；确认跳过后写入教程完成状态，并进入第 1 关正式关卡。
- 用户中途退出教程后，下次点击“开始关卡”时显示继续选择：继续教程或重新开始教程。
- 进入正式第 1 关时，棋盘应为空白正式状态，不继承教程中的 X、皇冠、提示高亮或冲突状态。
- 新手引导完成状态写入存档；如果用户清除存档或首次安装，则重新触发。
- 教程不应消耗正式关卡的免费提示次数、金币、生命或通关奖励。

当前实现：

- 已实现。新存档启动后直接进入新手引导；教程完成或跳过后进入第 1 关正式关卡。
- 现有棋盘点击、冲突校验和提示展示能力已复用，并新增 4 个规则场景教程棋盘、教程状态机、教程完成存档字段、中途退出恢复选择和进入正式关卡的流转控制。
- 顶部教程按钮只在教程中显示为“跳”；正式关卡页隐藏该按钮。

回归测试重点：

- 新存档首次启动必须直接进入新手引导，而不是停留在首页或直接进入第 1 关。
- 新手引导中必须隐藏返回首页按钮，只保留跳过入口；进入正式第 1 关后返回首页按钮必须恢复。
- 四步教程必须按顺序推进，未完成当前步骤不能进入下一步。
- 新手教程与正式关卡必须共用同一套区域颜色；区域颜色应清晰、鲜明、易区分。
- 格子引导必须先闪光聚焦目标，再出现小手；小手不得在页面进入瞬间抢先出现。
- 格子引导时必须显示蒙层，只保留当前目标格明亮高亮；用户完成该格操作、进入按钮教学或进入正式关卡后蒙层必须关闭。
- 新手引导词必须在上方教练提示区显示为大号深色字体。
- 每步教程完成后必须只出现轻量完成提示，不展示正式关卡结算弹窗，并在约 2 秒后自动进入下一步。
- 第 1 到第 3 个教程关必须隐藏底部撤销、清除、提示按钮。
- 放置皇冠教学必须只引导 1 个目标格，双击后直接放置皇冠并完成；单击不得放置皇冠。
- 颜色区域唯一教学必须预先放置一个皇冠，并逐个引导同色区域其它格标记为 X。
- 合并规则教学必须使用更大的 5x5 棋盘，预先在中间放置一个皇冠；必须先按顺时针引导周围 8 格标记为 X，再引导同行同列剩下的 4 格标记为 X。
- 第 4 个教程关必须显示撤销、清除、提示按钮，并按撤销 -> 清除 -> 提示的顺序解锁教学；撤销前必须有一个默认 X，撤销后变空白；清除前必须有多处尝试标记，清除后全部变空白；提示前棋盘为空，提示后出现高亮引导。
- 第 4 个教程关中，小手必须依次指向撤销、清除、提示三个按钮；当前按钮必须高亮，未到步骤的按钮退后或禁用。
- 提示教学中，点击提示不消耗正式免费提示次数和金币，也不自动落子；必须给出一个有效目标格，引导用户按提示双击目标格，用户完成后显示“已经了解全部规则，开始真正的挑战吧！”和“开始挑战”按钮。
- 通关目标说明必须解释每行、每列、每个颜色区域各一个皇冠，且皇冠不能相邻；同时说明进度反馈的含义。
- 点击“跳过教程”并二次确认后，必须直接进入第 1 关正式关卡，并保存教程完成状态。
- 中途退出教程后再次进入，应能选择继续当前步骤或从第 1 步重新开始。
- 教程完成后进入第 1 关正式关卡，棋盘状态为空白且无教程高亮。
- 教程完成状态保存后，重新启动再点击“开始关卡”不重复强制教程。
- 正式关卡页不显示教程按钮；首页“新人流程”按钮可用于重新体验新手引导。

## 7. 关键参数

| 参数 | 当前值 | 位置 | 说明 |
| --- | --- | --- | --- |
| 初始金币 | 55 | `scripts/main.gd` | 新存档默认金币 |
| 初始免费提示 | 3 | `INITIAL_HINT_COUNT` | 新存档默认提示次数 |
| 提示金币成本 | 5 | `HINT_COST` | 免费提示用完后的消耗 |
| 通关奖励 | 10 | `WIN_REWARD` | 首次通关奖励 |
| 默认棋盘尺寸 | 6x6 | `data/levels.json` | 当前关卡固定 6x6 |
| 默认关卡数 | 50 | `data/levels.json` | MVP 内置关卡 |
| 新手引导触发 | 新存档启动后立即进入 | `scripts/main.gd` | 完成后写入存档，后续不再强制触发 |
| 新手引导步数 | 单张教学地图 | `TUTORIAL_LEVELS` | 用同一张 5x5 地图串联双击放皇冠、周围排除、同行同列排除、同色唯一、提示线索和最终挑战入口 |
| 新手引导棋盘 | 5x5 | `TUTORIAL_LEVELS` | 教程全程不换图，让用户在同一棋盘上逐步找到全部皇冠；不污染正式关卡 |
| 棋盘区域色系 | 统一鲜明色系 | `REGION_COLORS` | 新手教程和正式关卡共用，保证视觉一致 |
| 新手引导跳过 | 允许 | `tutorial_skip_dialog` | 二次确认后视为完成并进入第 1 关 |
| 新手引导跳过入口 | 教程中显示 | 顶部“跳”按钮 | 正式关卡页隐藏 |
| 新人流程按钮 | 首页常驻 | `_simulate_new_user_flow()` | 用于模拟新用户状态并重新进入教程 |
| 教程完成反馈 | 找完教学地图全部皇冠后显示开始挑战 | `_show_tutorial_challenge_ready()` | 不展示正式关卡结算弹窗；用户点击开始挑战进入第 1 关 |

## 8. 发布前回归测试清单

每次正式发布前必须完成：

1. 运行核心冒烟测试。
2. 运行新手教程冒烟测试。
3. 手动验证首页主要入口。
4. 手动验证关卡页在目标移动端尺寸下不溢出。
5. 新存档手动验证启动后会直接进入单张 5x5 新手引导地图。
6. 手动验证首页“新人流程”按钮可重新进入新手引导。
7. 手动完成至少 1 个新手关、1 个普通关、1 个困难或专家关。
8. 手动验证提示不会直接放皇冠，并且文案解释原因。
9. 手动验证通关弹窗、下一关、重玩本关。
10. 如果修改了 `data/levels.json`，必须确认每关仍唯一解且 `logicStatus` 为 `no_guess`。
11. 如果修改了存档结构，必须验证旧存档兼容。
12. 更新本文档对应模块。

## 9. 自动测试命令

核心冒烟测试：

```bash
HOME=/Users/shingo_mac/Documents/Codex/2026-06-29/du-y/work/godot_home /Applications/Godot.app/Contents/MacOS/Godot --headless --path /Users/shingo_mac/Desktop/push_sudoku_shingosuper --script res://tests/smoke_test.gd
```

新手教程冒烟测试：

```bash
HOME=/Users/shingo_mac/Documents/Codex/2026-06-29/du-y/work/godot_home /Applications/Godot.app/Contents/MacOS/Godot --headless --path /Users/shingo_mac/Desktop/push_sudoku_shingosuper --script res://tests/tutorial_smoke_test.gd
```

## 10. 迭代更新规则

每次迭代按以下顺序执行：

1. 确认当前分支是 `codex-iteration` 或明确指定的开发分支。
2. 阅读本文档，确认要改的模块和影响范围。
3. 修改功能实现。
4. 同步更新本文档：新功能加入对应模块，行为变化更新功能逻辑，参数变化更新关键参数，新风险加入回归测试清单。
5. 运行自动测试。
6. 按影响范围做手动回归。
7. 提交代码。

## 11. 后续建议

优先补齐：

- 给提示系统增加可视化分层，例如“观察范围”“可排除原因”“建议操作”。
- 增加手机真机或 Godot 移动预览回归步骤。
- 将活动、排行榜、宝箱、任务等占位入口拆成明确版本规划。
